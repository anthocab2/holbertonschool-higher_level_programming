#!/usr/bin/python3
"""
2-safe_print_list_integers.py
Module to safely print integers from a list using try/except.
"""


def safe_print_list_integers(my_list=[], x=0):
    """
    Prints the first x integers of a list and returns the number printed.

    Args:
        my_list (list): list of elements of any type
        x (int): number of elements to access

    Returns:
        int: number of integers actually printed
    """
    count = 0
    for i in range(x):
        try:
            print("{:d}".format(my_list[i]), end="")
            count += 1
        except (ValueError
